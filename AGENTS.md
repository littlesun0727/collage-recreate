# 仓库协作与续接

本仓库用于开发 `collage-recreate`：运行在 OpenClaw 上的静态拼图复刻 skill。

## 开始工作

1. 完整阅读 `STATUS.md`，了解真实进度、当前授权和下一步。
2. 阅读 `PLAN.md` 的目标、边界，以及当前阶段。不要依赖之前聊天记录。
3. 查看 `git status --short`；保留用户已有改动。计划中的命令或文件不代表已经实现。
4. 按用户本次请求推进。只要求规划、解释或诊断时，不自行开始实施或付费生成。

## 项目边界

- 第一版只接收静态参考图，制作可换照片、可改文字、可调整主要元素的静态拼图。
- 生产主控是 OpenClaw + qwen3.8-flash；图像生成模型是用户指定的 qwen-image-3.0-pro。
- 不引入抽帧、运动分析、时间轴、视频或 Live Photo。不默认拆分为多 skill、多 Agent。
- `D:\codes\visual-recreate` 是只读借鉴来源，含用户未提交改动。不得覆盖、清理或重置。
- `D:\datas\0916live复刻2` 是只读样本来源。新任务产物放在独立任务目录。
- 不将样本的月亮、颜色、坐标、字体、图层数量写进通用实现或通用验收规则。
- 新 skill 最终须独立运行，不运行时导入旧仓库，也不提交密钥、客户照片、私有配置或远端原始响应。

## 证据与收尾

- 区分配置就绪、接口成功、OpenClaw 端到端成功、视觉通过、可编辑性通过。
- 整张生成图不能冒充可编辑工程；文件存在、schema 正确不能冒充视觉通过。
- 超时等结果不明的付费请求不自动重发；恢复已有请求状态后再决定。
- 每完成一个阶段或暂停前更新 `STATUS.md`：实际改动、验证结果、产物路径、剩余问题、下一步。
- 只有有验证证据的项目才能勾选完成。范围或决策变化时同步修改 `PLAN.md` 的决策记录。
- 计划文档面向开发续接，不要求运行 skill 的模型每次加载这些开发文档。
