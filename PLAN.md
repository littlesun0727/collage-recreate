# Collage Recreate：静态拼图 skill 实施计划

创建日期：2026-09-17。开发状态以 `STATUS.md` 为准。

本文是开发和跨会话交接计划，不是已经可执行的 skill 说明。本文中的拟议接口、目录和产物，须实施并验证后才可写入正式 `SKILL.md`。

## 1. 项目目标

输入一张静态参考图、客户照片和文案，由 OpenClaw 调度 qwen3.8-flash，必要时调用 qwen-image-3.0-pro，制作视觉接近参考、可修改的静态拼图。

第一阶段终点：当前参考对应的一张用户愿意使用的成图，附可换图、改字、调整主要元素的工程。第一版可复用性结论必须再经过不同参考验证。

成功要同时满足：

1. **画面成立**：构图、主要视觉层级与关键风格接近参考，客户内容正确。
2. **能够修改**：换照片、改文字、移动主要元素后可以重新渲染，不依靠重新生成整张图。
3. **真实自动执行**：在指定 OpenClaw + Flash 环境中完成，保留真实调用与产物证据。
4. **过程可解释**：明确耗时、生成次数、修正次数、人工介入及未解决问题。

新仓库隔离复杂度；新写 `SKILL.md` 本身不保证视觉质量。以真实行为和画面检验设计。

## 2. 范围冻结

### 第一版包括

- 单张静态参考；照片、文字、背景和装饰组合的拼贴海报。
- 常见裁切、缩放、旋转、透明度、遮罩及层级遮挡。
- 独立文本层、照片层和主要装饰层；可移动的相关元素组。
- 有需要才生成的装饰、纹理、背景素材，以及候选素材的视觉检查。
- PNG 成品、素材、可重新渲染的工程描述及最少使用说明。

“可编辑”首先指上述参数化工程可修改、可重新渲染；不承诺 PSD、Figma、网页拖拽编辑器或每一笔都可矢量编辑。位图装饰内部笔触不可独立编辑，应如实说明。

### 第一版不包括

- 视频输入、抽帧、运动分析、时间轴、动效、音频、Live Photo。
- 任意画风、任意复杂图片的像素级自动复刻承诺。
- 通用设计平台、插件市场、多 Agent 系统、多 skill 调度体系。
- 多生图供应商自动切换、未知结果的自动重试、无限候选探索。
- 没有当前用例支撑的数据库、队列、复杂工作流或模板市场。
- 自动去除来源水印；参考中的平台界面、标记不属于要重新制作的设计元素。

新增需求记入后续清单；不得为了“以后可能做视频”提前扩充静态数据结构。

## 3. 已有信息：哪些可用，哪些未证实

### 本地环境与路径

| 项目 | 路径或已观察状态 |
| --- | --- |
| 新仓库 | `D:\codes\collage-recreate` |
| 旧仓库，只读参考 | `D:\codes\visual-recreate` |
| 旧样本，只读参考 | `D:\datas\0916live复刻2` |
| 已有静态参考 | 样本下 `diagnostics\inspection\reference-004-full.png`，此前检查为 960 × 1280 |
| 旧成图 | 样本下 `previews\r000004-009bf69f\cover.png` |
| 原任务说明 | 样本下 `request-input.json`；先读取再确认客户素材与文字，不猜路径 |
| 新任务工作目录 | 建议 `D:\codes\collage-recreate-work\<task-id>`，独立于源码和旧样本 |
| 主控 | 检查时为 `bailian-token-plan/qwen3.8-flash` |
| 生图 | 用户指定 `qwen-image-3.0-pro` |

2026-09-17 的只读核查发现：OpenClaw 已能发现旧 skill；旧项目生图配置含有效的模型选择与凭证入口；本地代理健康检查成功；审计数据库中一次 qwen-image-3.0-pro 请求 HTTP 200、1 张图、7.736 秒。

**尚未证实**：OpenClaw 自主完成新 skill 的生图与组装、新的静态产物质量、可编辑性、重复运行稳定性。7.736 秒是一次服务耗时，不是整个任务耗时，也不是未来时延保证。

旧样本任务的 `permissions.services` 此前为空，不能直接改旧任务以“顺便开启生图”。新试验使用独立任务、明确的服务权限和调用预算。不得在计划或日志中复制密钥、完整服务配置、带签名下载链接。

### 旧样本提供的失败线索

此前视觉和文件检查发现以下问题，实施时要复核并转化为通用测试：

- 月相、蜡笔线条与相框的质感不成立；曾用程序绘制的近似素材替代关键风格。
- 字体气质不符；有局部坐标与画布坐标混用的装饰错位。
- 相框、照片与文字分组不完整，影响后续整体调整。
- 主控自评曾将明显风格缺陷降级为 warning。技术检查通过不能解决这个问题。
- 原背景与参考的留白、视觉重心不同；用户选用风景照片替换人物本身不是错误。
- 旧记录中的脚本执行时间不包含完整主控思考与等待，不能据此证明整个流程快。

这些是问题类别，不应变成硬编码某个月亮或某个坐标的“修复”。

## 4. 新旧代码关系

遵循 skill-creator 的范围和按需披露原则：一个静态任务、一个简短 skill 入口，可靠的重复动作交给脚本。开发文档不加入运行时必读列表。

旧仓库保留原状，包含未提交修改。审查工作区实际文件和依赖，不只看旧提交；每个迁移模块记录来源版本或内容哈希、采用内容和本地验证结果。迁移前检查适用许可证。

| 旧实现候选 | 可能复用的能力 | 必须审查的风险 |
| --- | --- | --- |
| `scripts/visual_recreate/media.py` | 静态图片加载、尺寸与格式处理 | 是否拉入不必要的视频依赖 |
| `render.py` | 图片适配、文本布局、RGBA 合成、静态变换 | 当前同时含时间轴逻辑，不能直接整体搬入 |
| `services.py` | Qwen/DashScope 请求、下载验证、请求状态与防重复提交 | 当前耦合旧 Task、Recipe、配置模型，需提取最小适配层 |
| `assets.py` | 素材派生与缓存思路 | 旧资产图和状态依赖是否超出静态需要 |
| `common.py` | 原子写入、文件摘要、受控报错 | 避免带入整套旧状态机 |
| 静态相关测试 | 裁切、文本边界、透明合成、换图与错误处理 | 移植有价值的行为断言，不复制旧 schema 负担 |

默认不整体迁移 `scene.py`、`models.py`、`master.py`、`vision.py`、`motion.py` 及原评审状态机。确有必要的静态算法可以提取；是否重写某个小函数，依据依赖和测试成本决定。

新仓库应自包含：不在运行时导入旧源码、不共享旧虚拟环境作为最终部署要求、不依赖开发机上的绝对安装路径。凭证采用环境或本地私有配置引用，不拷贝进 Git。

## 5. 最小制作流程与职责

运行时流程：理解参考和输入 → 确定静态布局及素材策略 → 准备必要素材 → 分层组装并预览 → 针对主要差异修正 → 导出。

### Flash 负责

- 查看真实参考、客户素材和生成/渲染结果。
- 判断主要元素、构图关系、视觉重点，以及哪些装饰需要生成。
- 给出清晰的素材指令和布局参数，使用稳定工具接口。
- 看过图之后才评审，优先修正最影响结果的差异。

### Qwen Image 负责

- 需要生成的装饰、纹理或背景素材，使用用户指定模型。
- 参考风格与客户内容的用途分别说明；仅发送当前生成所需的图片。
- 风格小样是候选，不自动成为最终素材。白底图不是透明 PNG，必须检查真实 alpha。
- 不能用整张生成图代替分层可编辑成品；准确文案与客户照片通常保留为独立层。

### 脚本负责

- 输入和路径校验、确定性裁切合成、文字排版、素材缓存、预览导出。
- 一致的坐标约定、稳定元素 ID、明确错误，以及最少必要的请求恢复记录。
- 自检尺寸、缺图、缺字体、透明度、文本溢出、重复 ID、边界和层级关系。

不强制所有素材走生图，也不以简易几何加噪点自动冒充参考的手绘质感。已有可用素材、普通几何和准确文字使用合适的确定性工具。

## 6. 实施阶段与出口

### P0 — 建立计划和续接入口（本轮）

- [x] 确定新仓库名称、静态范围和旧仓库保护边界。
- [x] 保存 `PLAN.md`、`STATUS.md`、`AGENTS.md` 与基础 `.gitignore`。
- [x] 核实关键样本路径，记录证据与未知项。

出口：新会话只读这些文件即可知道下一步。正式 `SKILL.md`、运行脚本和自动化尚未实现，不放置看起来可用的空技能。

### P1 — 最小技术设计和复用审计

任务：

1. 查看现有静态参考、旧成图、原请求和素材；不重新分析视频。
2. 检查第四节候选模块的依赖和测试，列出采用、提取、重写及暂不采用的内容。
3. 定义最小输入：参考图、客户素材、准确文案、输出尺寸、任务目录和服务权限。
4. 定义最小静态工程：画布、素材表、稳定元素 ID、图片/文字元素、层级、裁切、变换及必要分组。
5. 明确坐标规则。第一版优先画布坐标；分组若用局部坐标，必须由统一转换函数处理，不能让模型自行减父坐标。
6. 确定文字边界、照片填充方式、图层遮罩与源文件保留策略；超出能力的效果明确记录。
7. 确定工程版本、相对资源路径与最低限度更新校验，避免构建大事务系统。
8. 列出先写的行为测试和开发依赖；真实缺口才增加文件或抽象。

产物：简短复用结论、静态格式说明及测试清单。可在本计划追加落实记录，必要时再建立被实现引用的 schema 文档。

出口：能够解释一张图如何描述、如何修改、哪些函数真正需要迁移；不存在时间轴字段、样本硬编码或旧仓库运行时依赖。

### P2 — 最小静态工具和薄 skill

任务：

1. 建立独立 Python 项目、必要依赖和测试；版本根据实际兼容性选择并记录。
2. 实现图片、文字、层级、裁切、透明合成和必要分组的最小渲染链路。
3. 设计少量稳定操作，覆盖加载任务、素材生成、渲染/检查与导出；操作名经实现后再写入 skill，不照搬旧命令数量。
4. 输出精简结构化结果：成功/失败、产物路径、实际耗时、可处理的错误。大段内部状态不塞进主控上下文。
5. 将图片服务适配进同一个 task 流程，保留模型固定、缓存和不明请求不重发的安全特性。
6. 写正式 `SKILL.md`：目的、输入输出、制作过程、必须看图的节点、失败停止条件、按需文档链接。
7. 提供一个可执行示例；说明每个脚本做什么，不让主控临时拼 HTTP 请求或读源码寻找参数。
8. 完成离线测试：用少量本地测试图验证布局、文本、透明度、替换和错误返回；不消耗生图调用。

出口：确定性工具能够生成并修改一张简单分层图；skill 文档中的命令已真实执行；空任务、缺文件、中文/空格路径等报错可理解。此阶段不宣称视觉复刻完成。

### P3 — 真实 OpenClaw 的关键素材小样

任务：

1. 在当前授权范围内，把新 skill 接入独立测试环境，或明确批准的新测试会话；保留现有主控与旧 skill 配置，防止两套入口混淆。
2. 检查主控实际使用 qwen3.8-flash，工具可用、能收到图片证据；仅配置就绪不算验收。
3. 使用当前静态参考，让 OpenClaw 经新工具调用 qwen-image-3.0-pro。第一次调用尽量同时验证连通和有用的风格素材，不为证明连接额外生无关测试图。
4. 本例优先检验月相或蜡笔边框等关键质感；这是试验选择，不写入通用 skill。
5. 返回图片后，确认文件确实落地、可解码、主控确实查看，再判断风格和作为独立素材的可用性。
6. 记录指定模型、服务报告模型（若有）、请求标识、耗时、生成次数和脱敏日志。不能把配置中的模型名当响应证明。
7. 如为白底小样，明确标注；需要透明素材时，验证裁切/抠图结果、边缘和 alpha，再用于成品。

建议首轮只生成一个候选，明确问题后才做针对性修正。调用上限在任务开始时写入配置，执行范围仍以用户当次授权为准。

出口：OpenClaw 主控调用、图像服务返回、图片落地、主控看图和素材质量均有证据。若质感不成立，先解决这个阻塞，不组装成整张后宣布完成。

### P4 — 当前参考的完整静态主版

任务：

1. 根据已有静态图确定输出比例、留白、主次、照片位置和层级关系。
2. 明确素材来源及替换关系；客户选用风景照片时保留该选择，不能擅自换回人物。
3. 按需要准备背景和装饰；文字单独排版，客户照片独立绑定。
4. 合成低成本预览；OpenClaw 查看参考与预览，记录最影响观感的差异及位置。
5. 集中修正主要差异。初稿加最多两轮视觉修正可作为初始预算，停止不代表通过。
6. 生成正式尺寸成图，再查看正式尺寸结果，检查小字、边缘、缺图和错层。
7. 给用户展示对照与可编辑范围；用户意见反馈到通用流程或该任务参数，区分二者。

本样本验收重点：背景留白与视觉重心、月相形状与蜡笔质感、边框与线条、标题和小字、照片与标签对齐。下一个样本应从它自身参考重新确定重点。

出口：当前样本取得可信的视觉通过结论，附真实 OpenClaw 记录。预算到达但未通过时，保存候选并明确缺陷；不自动标记 accepted 或“已完成”。

### P5 — 编辑性与独立交付

任务：

1. 替换一张客户照片并重新渲染，验证裁切合理且无关元素没有变化。
2. 修改一处文字，验证可修改、字体可用、溢出能被发现。
3. 移动一组相框/照片/标签，验证相对关系保持，不出现局部/全局坐标错位。
4. 验证未改变的素材不重新生成；改文案不触发生图模型。
5. 导出成图、工程、必要素材、字体引用或合法可分发字体及最少操作说明。
6. 将交付包放到新目录重新渲染，验证不读取旧任务绝对路径、缓存和旧仓库。
7. 明确哪些内容仍是位图、不可细分编辑，哪些照片和文字可以直接替换。

出口：三类修改均有前后图和操作证据；新目录可重新渲染。单个 PNG 或无法脱离开发目录的 JSON 不算可编辑交付通过。

### P6 — 不同参考、重复运行和第一版发布

任务：

1. 选择第二张同属拼贴海报但布局或风格明显不同的真实参考；路径未定，不阻塞前期。
2. 冻结待测 skill 和代码版本，使用独立任务、同一主控执行，不为第二张先写样本分支。
3. 同时记录两类结果：新布局能否完成、是否需要人为修改提示词或脚本；需要修改就如实记失败/迭代。
4. 对关键流程做一次独立新任务复跑，并做一次原任务恢复/缓存复用测试；二者分别统计，不能把缓存耗时当首次生成耗时。
5. 汇总首次可见预览、全程耗时、服务等待、调用与报错次数、生成次数、人工介入和视觉结果。
6. 清理运行时必读文档，运行结构校验、行为测试和秘密信息检查；记录安装与实际验证环境。
7. 形成第一版结论：支持的范围、已通过的样本、仍需人工判断的部分、已知限制。

出口：至少两张不同参考经同一套 skill 完成，并验证恢复及编辑性；没有证据的泛化和稳定性不宣传。两个样本只支持有限范围结论，不等于任意输入可靠。

## 7. 测试、耗时和停止条件

### 三类验收不能互相代替

| 验收 | 看什么 | 证据 |
| --- | --- | --- |
| 技术正确 | 路径、尺寸、字体、裁切、透明度、越界、分组、模型请求状态 | 自动测试、检查结果和产物 |
| 视觉可用 | 构图、主次、关键风格、素材内容、文字观感 | 实际参考/结果对照及具体观察，用户反馈 |
| 可编辑和可执行 | 换图改字移动、跨目录复现、真实 Flash 工具链 | 修改前后图、运行记录和交付包 |

视觉评审必须基于实际看过的图；无法读取图片时，先报告能力阻塞，不臆测通过。明显错位、关键风格失真、错误素材、文字错误不能靠 warning 消解。用户尚未审阅时如实标记待审。

### 最少离线回归用例

- 竖图/横图填充不同槽位不拉伸；尺寸和焦点裁切可预测。
- 中文、英文、多行和长文本；缺字体、溢出可检测。
- 半透明素材、旋转、画布边缘、遮罩；不把 RGB 图误认成透明图。
- 分组整体移动与单独元素编辑；稳定 ID、重复 ID 和无效引用。
- 中文及空格路径；外部素材丢失、任务中断后读取和重新渲染。
- 无变化的素材缓存命中；改文案不请求生图；失败素材不会伪装成成功。
- 服务成功、明确拒绝、超时结果不明、已返回但下载失败；结果不明不重复付费提交。
- 导出后在独立目录渲染；缺失依赖能定位，不悄悄回退到开发机路径。

### 实际耗时记录

记录用户任务开始至首个可见成图、最终产物的墙钟时间；能区分时再记录主控、工具、模型等待和人工等待。至少保留调用次数、错误重试、图片生成、人工修正次数。先建立真实基线，不在未测量前承诺“一分钟”或“提速若干倍”。

优化顺序：移除无关视频流程与重复分析 → 固定常用操作接口 → 减少无效候选与工具错误 → 缓存确定性素材 → 根据实测瓶颈优化。无需每次调用前重复 doctor、help、完整源码和 schema。

### 必须停止或报告的情况

- 用户只要求规划/诊断，未要求实施：停在相应交付，不自动开始生成。
- 服务返回超时或不明状态：先核对既有请求，不盲重发。
- 到达约定调用或修正预算：保留候选、已花时间和缺陷，不伪造验收。
- 核心素材质感不成立：停止向后包装成“可用成品”，明确下一步选择。
- 需要更换模型、显著增加成本、修改全局配置或引入新系统：说明影响并取得相应授权。
- 第二样本暂缺：完成其他可验证阶段，明确泛化验收待补，不宣称全部完成。

## 8. 文件布局与续接规则

本轮真实建立的文件：

```text
collage-recreate/
├── AGENTS.md       新会话协作入口
├── PLAN.md         本文：范围、阶段、验收和决策
├── STATUS.md       当前进度、证据路径、阻塞与下一步
└── .gitignore      私有配置、产物及缓存排除
```

后续按实际需要增加 `SKILL.md`、`scripts/`、`references/`、`tests/` 和依赖声明。此处不预建空框架。新增脚本需要在参考文档中解释用途；文档必须对应已验证的真实接口。

任务私有目录保存参考、客户素材、布局工程、派生素材、预览和脱敏运行记录。仓库只存通用源码、说明及合法的测试夹具。交付包去掉凭证、带签名链接和不必要的私有分析记录。

每次交接前更新 `STATUS.md`：

1. 当前阶段与已经完成的具体动作。
2. 实際修改文件和测试命令、结果；没运行也要说明。
3. 输出路径及可检查的证据。
4. 未解决的问题、授权边界、下一步第一项操作。

未来线程先读 `AGENTS.md` → `STATUS.md` → 当前计划阶段，不能把计划条目当已实现，也不要重新讨论已经确定的静态范围。

## 9. 决策记录

### 2026-09-17

- 用户同意新开仓库；本轮要求详细计划落地，以便新会话继续。
- 采用一个静态专用 skill；旧仓库不动，必要工具按需提取，不整体复制。
- 第一版先支持照片、文字与装饰组成的拼贴海报。
- 主控和生图模型保持用户指定组合，不能以其他主控代跑结果冒充通过。
- 当前参考使用已有静态 PNG；不再为本项目开展抽帧和运动分析。
- 第一阶段追求一张可信、可编辑的成图；第二张参考用于检验通用性。
- 不在本轮创建远端仓库、提交凭证、安装新 skill 或修改 OpenClaw 全局配置。

后续变更应追加日期、原因、影响和验证结果；不为单次失败无限追加全局限制。

### 2026-09-17：P1 落实与 P2 开始

本次用户已要求实施。P1 现完成；格式落在 references/project.md，运行时不读取本计划。

- 实际查看静态参考、旧成图及六张客户照片；复核月相偏几何、线条过直、文字气质与背景留白差异。原请求只明确 title=Moon；客户照片选择继续保留。
- 旧仓库 HEAD dc5a04e；9 个已修改文件、3 个未跟踪文件。81 个已跟踪/未忽略文件的内容摘要存于独立工作目录 20260917-implementation/old-repository-baseline.json，收尾时比对。
- 未发现旧源码仓库许可证，所以没有迁移源码文本；以下为行为审计后独立实现。字体 DejaVuSans.ttf 依其独立许可证原样携带 LICENSE_DEJAVU，仅作可分发离线示例。

| 模块 | 工作区 SHA-256 | 处理与依据 |
| --- | --- | --- |
| media.py | c44fb926e42f448370eb61f67c3601a7bd3aa131aef5421d43177ac166e36b2f | 重写静态 EXIF/ICC/RGBA 加载；不引入 av/numpy；补充调色板透明度保留。 |
| render.py | 4bf4f277ee980cfd8bb36cfefda8630cf0a1c05172c9747defc7220353386f1f | 重写 Pillow 静态渲染；借鉴等比例 fit、alpha source-over 行为。弃用 cv2、时间轨、RenderPlan。 |
| services.py | c3bcaa9265d0fce20747d937af47449c8831c1aaf29f0b9964e436b155506b5d | 重写当前代理所需最小 JSON 接口；固定模型、请求前落盘、未知结果不重发、下载可恢复。不给旧 Task/Recipe 兼容层。 |
| assets.py | 3d3692f6ff8797a3a705ed681fcad87770ffaff05a3739077f1d19cba8428c41 | 仅保留内容摘要/不可变素材的设计；不引入资产 DAG、动态绑定和模型抠图。 |
| common.py | 9dfd1695c4e4be6af30db8566b352234c4d254d95d82a48fc93f01b259f3e102 | 独立实现 task 相对路径、原子写入、锁、紧凑错误；不移植修订目录状态机。 |
| 测试 | 已审查 test_pixels_time、test_remote、test_static_reuse、test_incremental_export、test_media_edges | 重写可观察行为断言；不照搬旧 schema 快照、视频或主版评审状态机。 |

最小决策：
1. 一个 project.json：画布、资源表、准确文案、稳定图层 ID、平面分组、权限；另存最少服务请求记录。
2. 所有元素始终写画布坐标，组只做 dx/dy 位移，统一放置；旋转绕元素中心，数组顺序决定层级。
3. cover/contain 保持比例；crop 为源图归一坐标；真实 alpha 遮罩，透明预乘后插值；超边界需显式 allow_clip。
4. 明确字号/框/字体，无静默缩小和替换；检查缺字、中文/英文/换行、溢出。字体不可分发时导出需求清单并支持显式本地字体绑定。
5. project version=1，revision 乐观冲突检测，原子更新；资源哈希不可变、路径均在任务内。原始输入保存在 private/originals，不入导出包。
6. Python >=3.11；实际使用独立 Python 3.14.6 环境。依赖固定于 requirements.txt；最终部署不使用旧仓库虚拟环境。
7. 先做离线裁切、已知半透明像素、旋转隐藏色、分组与换图/改字、错误原子性、中文路径、独立导出，以及服务成功/拒绝/未知/下载恢复/缓存/预算测试。
8. 第一版不做自动抠图。核心素材若需要透明化，须另有经检查的 alpha 素材或明确可验证的局部处理；白底小样不能当透明素材。

P2 的完成以实际命令和测试结果为准，记录在 STATUS.md；当前格式文档不是视觉通过证据。

### 2026-09-17：P2 验证完成，P3 等待一次具体调用授权

- P2 已实现，最终 49 项离线行为测试通过（11.46 秒）；机器报告位于独立工作目录 offline-tests.xml。skill-creator 结构校验通过，全部运行脚本编译通过。
- 新增生成目标检查：提交前拒绝覆盖参考图或字体；生成结果绑定前再次验证静态工程。该问题在离线代码检查中发现并加入两个回归用例。
- 已通过独立导出 runtime 重渲染；旧仓库 81 个文件的内容摘要、旧图像配置和 OpenClaw 全局配置均保持不变。
- P3 使用现有 DashScope 协议；建立独立 workspace/state，并已离线导入参考、冻结提示词与一次调用预算。具体请求见工作目录 openclaw-p3/APPROVAL.md、generation.json、request-preview.json。
- 启动 P3 的命令被自动审批拒绝。拒绝理由是当前用户实施请求未被认定为明确授权这张参考图向外部服务的上传和可能付费的生成；不得通过间接执行绕过。
- OpenClaw 实测尚未启动，生图请求计数为 0。需要用户确认上述具体调用后，才运行 run-p3.py；这次批准将覆盖主控 Flash 看参考和经现有代理向 DashScope 提交最多一次、单张 1024×1024 小样。
- P3–P6 仍未完成。不将离线工具通过、配置声明或准备好的文件冒充真实端到端、视觉或当前样本编辑通过。


### 2026-09-17：P3 获明确批准并实测，视觉未通过

- 用户明确回复“批准本次P3实测”，原审批阻塞解除；仅执行已准备的单张候选，没有增加次数或上传客户照片。
- OpenClaw 2026.9.4 的独立 agent exec 已运行，session_id=8e044c2e-141c-49f8-af99-554b046072d4。SQLite 的 12 次 assistant 响应均记录 provider=bailian-token-plan、responseModel=qwen3.8-flash。
- 主控实际读取新 skill，调用新工具并两次接收 image 内容：参考及候选。候选传入模型的 PNG 哈希与落盘文件一致；这次证据超过“配置支持图片”或自报已查看。
- 唯一图像请求 HTTP 200、completed、attempt=1，request_id=83d019bd-c8be-484f-93f8-56c24046be2f；requested_model=qwen-image-3.0-pro，reported_model=null，不混淆请求选择与服务报告。
- 生成服务耗时 10.78 秒，generate CLI 11.544 秒，整个 runner 174.778 秒。重复 inspect 带来额外开销，其中一次探读尚不存在的账本文件导致 shell exit 1；没有额外付费重试。
- 候选 1024 × 1024，RGBA 但 alpha=[255,255]；七个月相为白底不透明小样。素材 SHA-256 为 345fd0aac8d89551eacbc49487c4b78fc84c0e86348ae8ce5065c62980e3bf46。
- Flash 和当前助手实际看图后均判定质感仍有明显差距：干涩排线、轮廓偏硬、颜色偏淡、月牙偏瘦；参考需要更柔和饱满的厚涂与光晕。review.json 标为 needs_revision，P3 不勾选完成，不推进 P4。
- 模型完成评审后正常 stop；OpenClaw 清理阶段却返回 “Agent runtime cleanup did not settle”，最终 runner exit 1。安装代码证实此路径会替换原成功 envelope；会话仍保留实际调用与最终文字。具体清理步骤尚未定位，未修改全局配置/安装，也未为排查重发生图。
- 既定 1 次预算已经用完，保留原 task 和证据。下一次定向修正提示词已离线存入工作目录 generation-revision-proposal.json，尚未授权提交；不能通过修改预算或新建任务绕过边界。
- 实测后核验：旧仓库 81 个文件及旧图像/全局 OpenClaw 配置无变化，冻结 skill 的 18 个文件和新仓库运行文件无变化，旧参考副本一致。证据见 p3-evidence.json 和 p3-preservation-check.json。
- 本次没有修改通用运行时代码，未重复运行 P2 的 49 项测试。只更新阶段记录、授权记录，并新增本地实测、审计及下轮请求草案。


## 10. 当前实施：A1 参考分析规范与 Flash 单步实测

2026-09-17 用户明确要求：新增分析规范，列出文件变更计划并落盘实施，再用提供的复杂静态参考执行 OpenClaw + qwen3.8-flash 实测。本节优先于旧的“下一步继续修正月相提示词”安排。

### 范围与产物

只完成制作第一步“参考分析”：主控自行观察原图、按需看局部、记录可见事实和不确定项，输出分析报告。素材/图层工程拆解属于后续 A2，本次不制定生图提示词、不生成素材、不创建整版布局。旧 P3 已验证的接口证据保留，但不视为自主分析能力证明；其中 generation.json 由 Codex 预先编写。

### 文件变更计划

| 文件 | 动作 | 目的 |
| --- | --- | --- |
| SKILL.md | 修改第一步及按需读取入口 | 新参考先读 analysis.md，明确产物；分析限定任务完成后返回。 |
| references/analysis.md | 新增 | 观察范围、整图/局部查看、事实与推测区分、报告格式和完成条件。 |
| scripts/inspect_reference.py | 新增 | 独立任务初始化之前即可读取静态图尺寸、摘要及按像素坐标导出局部；不做模型分析、不调用远端。 |
| references/tools.md | 补充 | 说明上述辅助工具用法，区分图片查看与工程 inspect。 |
| references/project.md | 小幅修改说明 | 明确它描述可执行工程，不是参考分析规范；既有 schema 不变。 |
| tests/test_inspect_reference.py | 新增 | 验证裁切像素/原图坐标、源文件保护、无效区域及已有输出保护。 |
| PLAN.md、STATUS.md | 持续更新 | 记录范围调整、实施状态、实测证据及尚未通过的内容。 |
| 独立工作目录 | 新增私有产物 | 基线、冻结 skill、输入副本、独立 OpenClaw state、原始报告与审计评审。 |

工作目录：D:\codes\collage-recreate-work\20260917-reference-analysis。
代码基线：用户已保存的 87c6fd7。旧仓库、旧 P3 实测目录、用户给定原图及全局配置保持原样。

### 执行顺序与验收

- [x] A1.1 写入短分析规范、入口和最小查看工具。
- [x] A1.2 验证本地工具与 skill 结构；文档中的命令实际执行。
- [x] A1.3 在独立 workspace/state 冻结版本，准备复杂样例与无答案任务指令。
- [x] A1.4 用 OpenClaw + qwen3.8-flash 只执行第一步；保留实际模型、读文档、图片接收、工具行为和报告。
- [x] A1.5 对照原图评价报告：覆盖、空间关系、关键形状与文字、不确定性、定位可追溯；更新续接状态。

测试任务只提供参考图、当前 skill、解释器位置与“仅分析”的用户目标，不提供月相数量、文字答案、区域坐标、预写分析或前次评审。主控的区域选择和描述必须来自它实际看到的图。具体样例评分记录放在测试 workspace 之外，避免作为答案注入。

本次用户已授权该图片由指定 Flash 模型分析；测试会使用现有主控服务，但不调用 qwen-image，不增加生图预算。首轮完整执行后先审核事实；如发现可修复的规范/工具缺口，最多进行一次保留首轮证据的定向修订与独立复测。没有改动依据时不反复调用。

最终分别记录工具正确、模型与看图链路、分析质量、runner 退出状态。一个复杂样例只支持本例结论；不宣称泛化、工程拆解或整版编辑性通过。原 OpenClaw 清理异常如再次出现，以保留会话核查实际完成情况，不用退出码替代所有结果判断。

### 编写依据

采用已调研的官方原则：适度具体、按需读取、明确输入输出、真实任务迭代。它们不保证 qwen3.8-flash 的实际效果，仍以本次执行为准。

- https://agentskills.io/skill-creation/best-practices
- https://learn.chatgpt.com/docs/build-skills
- https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices

首轮 A1 实测：工具/看图链路有证据，报告存在方向、关系和不确定性处理问题。保留 review-01.md；仅定向修订 references/analysis.md 并准备独立 run-02，任务提示词与输入字节保持不变，未加入样例答案。

### A1 收尾结果

A1.1–A1.5 的实施、验证和独立评审已执行；这不等于分析质量通过。两轮报告均保存且未由 Codex 改写。第二轮修正月相方向，但主连接计数/归属仍有错误，质量保持 needs_revision，后续 A2 未开始。统计和具体问题见工作目录 RESULTS.md、review-01.md、review-02.md。两轮分别 174.361 秒/13 次工具和 319.132 秒/23 次工具；均为实际 qwen3.8-flash，均无生图。OpenClaw 清理异常仍未修复。

## 11. 当前实施：A1 JSON 分析与可视化复核

2026-09-17 用户确认：规范保留 Markdown，正式分析结果使用 JSON，人工查看由 JSON 生成；继续使用给定复杂参考，经 OpenClaw + qwen3.8-flash 实测。本节为当前实施范围，优先于第 10 节的 Markdown 输出安排。A2 工程拆解仍为后续。

### 文件与产物计划

| 文件 | 动作与职责 |
| --- | --- |
| references/analysis.md | 改为 JSON 输出规范，保留形状、方向、风格、关系、文字与不确定性；说明原图坐标和看图复核。 |
| SKILL.md、references/tools.md、references/project.md | 更新按需入口、实际命令和分析结果与工程的职责衔接。 |
| scripts/collage_recreate/analysis.py | 新增独立分析数据契约、来源/坐标/引用/证据校验、JSON 派生预览。复用既有 Pydantic/Pillow，无新依赖。 |
| scripts/review_analysis.py | 新增 init/check/preview 命令；init 由程序写原图元数据，Agent 填观察；check 不写分析，preview 派生编号图与可点击 HTML。 |
| tests/test_analysis.py | 验证实际行为：中文长描述、错误定位、关系和裁切来源、保护原图/分析与已有输出、CLI 和派生结果一致性。 |
| PLAN.md、STATUS.md | 更新实施、测试、质量和运行异常。 |
| 独立工作目录 | D:/codes/collage-recreate-work/20260917-analysis-json；保存当前未提交基线、运行快照、原始 JSON、审计和评审。 |

### 顺序与验收

- [x] J1：实现 JSON 契约、校验与编号/交互预览，更新规范及入口。
- [x] J2：离线验证和实际命令检查；预览可从 JSON 重建，不让 Agent 双写 Markdown。
- [x] J3：冻结独立 workspace/state，用同一复杂参考运行一次真实 OpenClaw + qwen3.8-flash，仅分析。
- [x] J4：审查实际 JSON 与预览，核对模型、工具、图片接收、耗时；记录质量和 runner 状态。

不预填样例答案、数量、方向、坐标或工程决策。具体样例评审在测试 workspace 之外。技术校验不能判定观察正确；模型自评不能替代人工核图。对重要关系/方向仍不确定时保留疑点。一次运行可按工具报错修正 JSON 并在同一会话复核；本轮不自动启动第二次模型运行，不调用生图、不进入工程拆解或整版制作。

当前 code HEAD 为 87c6fd7，并带上一轮 A1 未提交变更；在此基础上继续，不重置或覆盖旧实测。全局 OpenClaw 配置、旧仓库及原参考保持原样。

### A1 JSON 收尾结果

J1–J4 均已实施并留有证据。79 项离线测试通过（14.41 秒），交互预览经实际浏览器验证。本次唯一一次 OpenClaw 2026.9.4 + qwen3.8-flash 运行耗时 484.48 秒、55 次工具调用；生图 0。15 元素/9 关系/16 视图/3 疑点的最终 JSON 通过结构检查，与 review-03 预览一致。校验发现并促使主控修正自引用、字段/类型、未知端点和误删证据，但月相方向、框选范围和关系颗粒度仍有问题，质量 needs_revision。模型正常完成后 runner 再次在清理阶段 exit 1。详情见本轮工作目录 RESULTS.md、review.md、metrics.json；A2 未开始，不追加本轮第二次模型运行。


## 12. 当前实施：简化为制作草案

2026-09-17 用户要求采用 figcopy 的业务结构简化并实测。本节优先于第 10/11 节：第一步直接输出 background / slots / overlays / layer_order / questions，不再维护抽象观察与关系图。保留简单 attachment；来源与校验摘要由工具产生。草案包含制作分类，实际资源准备、工程组装和编辑性验证仍是后续工作。

| 文件 | 本轮变更 |
| --- | --- |
| references/analysis.md | 五字段制作草案，原图 xywh 坐标、背景意图、拆分尺度、归属与复核。 |
| SKILL.md、references/tools.md、references/project.md | 更新入口、命令和草案如何进入资源准备/工程组装。 |
| scripts/collage_recreate/analysis.py、scripts/review_analysis.py | 移除抽象图结构及分析 init，改为草案校验、附属层自动展开和交互预览。 |
| tests/test_analysis.py | 用槽位、归属、背景、层级、预览一致性等真实行为替换旧图结构测试。 |
| PLAN.md、STATUS.md | 当前范围、实施及实测结果。 |
| 独立工作目录 | 新增 D:/codes/collage-recreate-work/20260917-simple-draft，保存改前版本、冻结 skill、原始模型产物与评审；旧结果不改。 |

- [x] D1：规范、工具、预览与工程衔接改造。
- [x] D2：离线测试及实际浏览器检查。
- [x] D3：同一复杂原图，一次独立 OpenClaw + qwen3.8-flash 制作草案实测。
- [x] D4：独立核图、时间/工具统计、原始结果留存及状态更新。

本轮不生成素材、不组装成图，不改 OpenClaw 全局配置。测试仅提供原图、当前 skill 与目标，不提供旧答案或样例拆分；无额外客户替换要求，不能沿用 figcopy 旧样例中“客户确认换背景”的前提。裁切工具仍支持原有 LTRB，草案明确采用 xywh。简化结构不意味着视觉判断自然正确，分别评审结构、业务拆分、视觉和 runner 状态。


### 简化制作草案收尾

D1–D4 完成：五字段规范/校验/交互预览及衔接说明已实现，97 项离线测试通过；真实 OpenClaw + qwen3.8-flash 单次实测耗时 350.945 秒、39 次工具调用、生图 0，产出 7 槽位、18 装饰、6 疑点。三次 check/三次 preview 均通过，全部 17 个裁切与原图匹配并送达模型，最终 review-03 与 draft 一致。39 次工具内含三项命令/路径错误，无草案 schema 返工。

本例制作质量仍 needs_revision：短划颜色与数量、部分范围截断、未知标识提前删除；装饰拆分仍可收敛。模型正常结束后 OpenClaw 清理失败，runner exit 1；未改全局环境，未自动重跑。详细产物与独立评审位于 20260917-simple-draft/RESULTS.md、review.md。

下一步应保留业务草案结构，先纠正局部范围与制作取舍、收敛共同制作的照片装饰，再准备真实资源/字体/客户内容进入工程组装。不要把草案直接改名为 project.json，也不要把 reference_generate 项数直接转为生成请求。

## 13. 修订方案（已实施，结果见第 14 节）：背景策略、图片与文字分离、装饰合并、定位细化

2026-09-17 用户提出四项问题，并要求先分析如何修复。本节保留当时的方案；用户随后已授权实施与测试，完成结果见第 14 节。当前运行格式以 references/analysis.md 和代码为准。此前“保留五字段”及“未明确换背景就清版”不再作为下一轮设计约束。旧实测结果保留，不重写历史评审。

### 背景先按内容分类

- 风景、生活照、人物照等摄影背景：默认规划客户图片背景槽；有合适的客户素材时绑定，没有时保留待绑定槽和素材需求。无需先对参考照片大面积去字、去框、补景。
- 格纹、报纸、纸张、印刷纹理等设计底板：走固定背景分支；有干净可复用底板优先复用，有覆盖内容时才清版或重绘。纯色/简单渐变用确定性工具。
- 客户明确保留原摄影背景或指定替换时，以该要求优先。混合背景先区分摄影底图和前景设计装饰，不因存在纹理就清版整张。
- 背景分类由 VLM 结合原图判断；字段校验不能证明分类正确。不把背景槽规划误写成客户已经确认了某个具体文件。

### slots 与 texts 分离

- 建议顶层为 background / slots / texts / overlays / layer_order / questions。
- slots 只表示客户图片；移除冗余 type=image 和文字专用字段，保留 photo/photo_feather/cutout/unknown 等图片准备方式。
- texts 表示可编辑文案，保存准确文字或 null、参考位置及必要样式说明；不再携带图片 mode/upload_hint。layer_order 增加 text 引用，ID 跨三个数组唯一。
- 固定手写装饰字可与装饰共同制作，仍记录准确 text_content；这是不可单独改字的位图素材。已在客户文案/可改名单中的字必须保持独立文字层，不能为减少素材数而压平。共同移动用工程 group；共同移动不必共同栅格化。
- check、计数、编号、筛选、点选和工程衔接一并更新，避免只改提示词而旧 schema 仍要求混合槽位。

### 按独立制作需求决定合并

- 一个 overlay 是一份需要独立制作和摆放的装饰素材。相同归属、共同移动/缩放、相同外部遮挡关系、没有独立编辑要求的局部装饰，默认合为一个 overlay，允许内部包含不相连笔触。
- 必须拆分的条件：归属不同照片；需要独立修改/移动；分别位于照片前后；中间需穿插其他层；合并后范围过大导致局部细节无法保持。不得仅因颜色不同或形状不连通而拆分。
- attachment 继续表达归属；合并必须直接减少 overlays 条目及独立素材数，仅让多个条目使用相同 attachment 不算合并。
- 本例候选：每张照片的边框、固定季节标签、侧边短划各合成一份；Moon 与两侧短划可合成一份固定装饰；大小星星可合成一个星群。四季分别保留四份装饰，月相、跨对象吊线、半透明色块分别保留。
- 本例在固定装饰字前提下可从 18 项装饰收敛为约 9 项；这是样例候选，不是通用数量上限。Moon/季节字若需可改，则使用独立文字层和分组方案。合并边框素材的照片开口必须透明，不得将原照片烘焙进去。

### 一套参考坐标与批量定位细化

- 草案删除 target_rect，保留 source_rect=[x,y,width,height]，统一为朝向归一后的原图像素。目标布局只在 project.json 的现有 x/y/width/height 等字段中表达；改输出尺寸或移动元素时由确定性映射/工程更新处理。
- VLM 输出语义分类、制作组合和大致 source_rect；不要求它反复估计像素级边界。先完成组合，再按组合定位。照片框指照片内容区域，装饰框指整份装饰的可见范围。
- 建议增加 scripts/refine_layout.py 及对应模块：只读原图和草案，批量输出新 refined 草案、定位诊断与粗框/细化框对照预览。原草案和旧预览保留；细化草案每项仍只有一个 source_rect，粗框、候选 mask/轮廓和算法参数留在程序诊断中。
- 照片：在扩大的局部搜索区内检测边缘/矩形候选，区分照片边界与外侧手绘框。文字：文字检测提供候选框并匹配对应文案位置，识别结果不能直接覆盖客户准确文案。装饰：按语义范围结合颜色、边缘、连通区域寻找完整笔触，多个连通区域可以属于同一组合，不能只取最大区域。
- 先采用有证据的局部方法；颜色相近、交叠、光晕和细线均可能失败。必要时再评估带提示的分割，不能把任意粗框交给一种算法就宣称精确定位。OCR/分割依赖是否加入需先核对现有环境和样例收益，本轮未安装。
- 搜索区先留余量，检测触边时有限扩展；仍触边、多个候选难区分、无候选或参数轻微变化导致范围大变时，标记 needs_review 并保留原候选，不静默升级为精确值。裁切/缩放坐标由脚本映射回原图。
- 固定算法参数、随机种子（如使用）、版本及输入摘要；按原图与相关元素输入/参数缓存，批量处理，只重跑变化项。定位诊断 mask 不自动作为生产抠图素材，遮挡后不可见边界不能宣称已经恢复。
- 当前工程支持旋转与遮罩，不支持透视；定位得到的四角不代表已新增透视渲染能力。首版不顺便扩展渲染器。

### 建议实施顺序和验收

1. 先改业务规则、草案 schema、校验、预览和工程衔接；保留旧产物/冻结运行时，不批量覆盖旧草案。
2. 再做定位脚本的离线原型：人工标注少量参考范围作为评估基准，测试照片内框、文字尾笔、分散星群、粗框截断和同色背景等真实失败类型；不能只证明 JSON 有效或框在画布内。
3. 用当前摄影背景和新增纹理底板参考验证路由与通用性，比较定位误差、漏笔触、需复核比例、墙钟时间和工具次数。不能先承诺比现有流程快或任意图片像素级准确。
4. 离线结果成立后，再按后续明确执行范围进行新隔离 Flash 验证；只要求方案的本轮不启动主控、不生成素材、不执行旧 runner。

实施位置：references/analysis.md、scripts/collage_recreate/analysis.py、scripts/review_analysis.py、SKILL.md、references/tools.md、references/project.md；拟新增 refine_layout.py 及行为测试。inspect_reference.py 目前仅读尺寸/裁切，可继续复用其无损裁切能力。

算法能力参考（仅用于设计，未在项目验证）：[OpenCV 轮廓与包围框](https://docs.opencv.org/4.13.0/dd/d49/tutorial_py_contour_features.html)、[GrabCut 的前景/背景提示与修正要求](https://docs.opencv.org/4.13.0/d8/d83/tutorial_py_grabcut.html)、[PaddleOCR 文字检测](https://www.paddleocr.ai/main/en/version3.x/module_usage/text_detection.html)。

## 14. 四项修订实施与两图验证完成

2026-09-17 用户要求按第 13 节更新并测试，随后明确批准原参考与本地格纹参考发送给配置中的 Flash，一次隔离会话、无生图。工作目录 D:/codes/collage-recreate-work/20260917-refine-draft，旧产物/原图/全局配置保留。

- [x] R1：六字段草案、摄影背景槽/纹理固定背景、图片与文字分离、单 source_rect、独立制作组合、预览与工程衔接。
- [x] R2：refine_layout.py / localize.py；局部边缘、颜色与连通区域、有限扩展、缓存和批量输出。失败保留粗框并标 needs_review；诊断 mask 不作为生产素材。
- [x] R3：最终 116 passed in 16.13s，含 14 个定位测试；两份复核版 Edge 交互检查通过；skill 格式/脚本编译通过。定位可选依赖 NumPy 2.5.3、OpenCV 4.14.0.94，单独安装，要求 Python >=3.12；基础运行时仍独立。
- [x] R4：真实 OpenClaw + qwen3.8-flash 两图一次会话，455.485 秒、59 次响应、63 次工具、10 次报错、0 生图。13 次裁切逐像素匹配；模型正常结束后 runner 清理失败 exit 1，不重复执行。
- 实测显示 A 摄影背景为图片槽、B 格纹为固定背景；slots/texts 正确分离；A 装饰从上一轮 18 项收敛为 9 项，但星群仍分开，局部范围和月相说明有误。原始 B 用 basic_shape 表达带字标签，后续校验已禁止。
- 实测后增加边角连续性、邻居重叠、文字混色和边界敏感性检查，以及具体图层错误消息和 inspect 参数别名。最终修复仅离线/本地验证，未追加模型调用。
- Codex 独立复核版保存在 reviewed/，原始 Flash 输出在 run-01/analysis/，不混淆来源。最终本地定位复核稿 A 为 5 采用/10 待复核/1 不变，B 为 3 采用/3 待复核；复杂区域没有全部自动精确通过。
- 本轮未引入 OCR/语义分割、透视渲染，也没有执行素材生成或工程组装。第 13 节算法方向以已实现的保守局部方法为界，不能宣称任意粗框都能自动精确定位。

详细结果 RESULTS.md、独立评审 review.md、最终 JUnit 和 preservation-check.json 位于上述工作目录。

## 15. 最新：校验耗时诊断与精简建议（未实施）

用户认为识别效果可以，要求查看校验流程过长的原因。只读审计同一次会话：两份初稿 105.287 秒完成；图层结构排错/首次定位 107.675 秒；后续复核、编辑、重复定位与收尾 242.523 秒。两图各三轮成功 refine；B 第二轮输入/输出不变、全量命中缓存。算法进程约 1.1–2.4 秒/单张/轮，主要耗时来自多轮模型与工具往返。

根因：
1. 背景槽与固定背景的图层写法例外，旧错误消息没有列出具体缺失/多余项，导致模型查源码返工。
2. needs_review 没有明确停止规则，模型追求不断修正，而非返回可用草案和局部疑点。
3. refine 已包含校验/预览，模型仍额外 check、重读诊断、查看多个未变版本。
4. 开发回归/浏览器检查是此轮开发验收，不能放进每次客户任务。

建议下一次获准实施时：
- 正常路径固定为看图→一次草案→一次 refine→一次看图→返回，只有重大问题再做至多一次定向修正。
- 结构错误返回具体对象、修法和可同时发现的所有错误；无输入变化不再 refine，正常客户任务不读实现源码。
- needs_review 可作为局部不确定性随草案交付，不要求全部变绿；遗漏主元素/明确越界等阻止使用的问题单独处理。
- 简短机器摘要直接给出需关注项和预览路径，完整诊断留档。避免重复 check/preview 和逐项读取。
- 评估由程序确定性展开背景与附属层，保持层级意图可审阅，减少模型要记忆的格式例外。

详细证据见工作目录 LATENCY.md、latency.json。以上精简策略尚未实施，不预报未经实测的提速比例；本轮不自动启动第二次 Flash。

### 草案分析阶段的明确停止条件（建议，尚未实施）

适用范围仅为参考分析和定位，不扩展到后续真实素材生成、工程组装或最终交付检查。

#### 正常路径与预算

看参考 → 写草案 → 调用一次 refine（内部校验、定位、预览）→ 看一次预览 → 返回。

每张参考图在本次分析任务内共用一次修正机会，结构排错和视觉修正共用这一个预算：
- refine/结构校验尝试合计最多 2 次，初次失败也算一次。
- 预览查看最多 2 轮，第二轮仅看改变的对象及其受影响邻居。
- 首次预览后最多追加 1 批针对已发现问题的局部裁切，不全面逐项重看。
- 第二次检查后结束：可用则交付，不可用则报告阻塞项；不进入第三次自动修正。

#### 允许修正的触发条件

只有具体、会影响下一步制作的问题可以消耗修正机会：JSON 无法读取、ID/引用/图层错误、明显越界或空框、主要照片/文字/装饰遗漏或归错类、框明显选中了其他对象、客户明确要求被违反。修正前写清对象、已有证据和预计改变，不能只写“再优化精度”。

轻微边界偏差、细笔触/光晕不确定、算法 needs_review、缺少待提供素材或未确认文案，均可保留具体疑点后结束，不要求全部变为 refined。不确定的装饰分组可给出当前最合理组合和待确认项，不循环试各种拆法。

#### 正常结束条件

草案结构有效，主要对象已覆盖，图片/可改文字/固定装饰归属明确，可生成审阅预览，且无已知阻止下一阶段使用的错误，即返回。
局部定位或识别不确定时，保留当前可靠粗框、注明对象及影响，状态为“可用，有待确认项”。不能将该状态表述为视觉精确通过。

#### 立即停止重复检查

满足任一项就停止：
- 参考与相关输入未变；旧结果可直接复用，不再启动模型检查或定位。
- 第二次出现同一问题，或已经用完一次修正机会。
- 没有新证据能说明下一次操作会解决哪个具体问题。
- 继续操作需要反复猜颜色、调整阈值、读取实现源码或探测内部函数；这些属于开发排错，转为记录问题。
- 工具/依赖错误在允许修正后仍阻塞；给出可复现错误和现有草案，停止现场调试。

#### 返回状态

- 可用：草案有效，没有已知影响当前制作的疑点。
- 可用，有待确认项：草案有效，局部仍不确定；列对象和影响。
- 草案未就绪：仍有结构错误或明确遗漏等阻塞项；保留已有结果，说明需要处理什么。

前两者正常结束；第三者也结束本次自动分析，但不能冒充成功。

#### 实施方式

提示词和脚本共同约束。将尝试次数、预览轮数、输入摘要、上次错误和任务终态写入同一份任务状态；按本次任务与参考标识累计，换文件名、输出目录或修改诊断说明不能重置预算。只有用户发起新的修改要求，才开始新的修正任务。

正常流程直接使用 refine 的简短摘要和预览；不额外 check/preview、不常规读取完整诊断或实现源码。不将 pytest、浏览器自动化及开发审计纳入客户运行流程。

这套建议尚未写入生产规则或脚本，未追加模型实测；用户当前要求是讨论明确停止条件。


## 16. 停止规则实施（上一轮；最新见第 19 节）

用户已明确要求先提交、再更新规则并试跑。已先创建当前版本提交 e20769d；本次新增改动暂不再次提交。正常流程一次 refine → 一次看图 → finish；每张参考共用两次检查上限，错误也计数，修改需要具体 issue/reason，未变输入直接复用，finish 后关闭。CLI 共享任务状态并使用文件锁；宿主可锁定任务根目录，避免换目录清空预算。直接看图仍由 skill 规则约束，通过真实 transcript 验证遵守情况。

先离线验证行为，再同样两图一次新隔离 Flash；无生图、其他素材上传或全局配置修改。比较实际检查/看图轮数、工具数、耗时与原始草案质量，不为提速隐藏未解决疑点。修正规则写入 references/analysis.md 与 tools.md，结果记录在本轮独立工作目录。

### 当前验证与执行边界

- 当前基线提交 e20769d 已完成；停止规则代码/说明更新尚未再次提交。
- 最终离线结果 131 passed in 16.97s；新增 15 个停止行为测试，覆盖次数共享、失败计数、重复输入、终态、缓存完整性、并发与中断。
- skill 格式、脚本编译和差异检查通过。冻结运行快照 26 文件，保持图片与旧结果隔离。
- 本次新 Flash 启动被自动审批拒绝，认为此前明确图片外发授权仅限上一轮单次会话。未启动进程、未发出请求；已请求本次新的两图单次会话授权，等待用户答复。不能换启动方式绕过。

### 实测中的前置观察缺口与补充

获用户“批准本次新的两图 Flash 实测”后，本轮唯一一次新会话已启动。冻结版本只限制草案检查，却没有量化初稿前观察。真实主控在初稿前反复裁切同一区域，并另写多段像素扫描/颜色阈值代码，尚未进入受预算控制的 refine。这属于新的、实际观察到的耗时来源。

因此当前工作区补充：每张参考初稿前一次全图、最多一批 4 个必要局部，同一区域不反复重看；到上限必须写粗草案，不明确处保留 null/粗框/questions；不得另写像素扫描或定位代码，精定位统一由 refine 承担。本轮正在运行的冻结 skill 不修改；该补充没有第二次 Flash 验证，不计入当前实测效果。


### 第 16 节实际收尾

用户再次明确批准后，唯一一次新 Flash 已执行，前述等待授权状态已解除。131 项离线通过，脚本两次预算生效；真实运行 613.947 秒超时（runner exit 2），59 次工具、3 次工具错误、0 生图。A 在一次结构修正后 finish，B 修改图层引用后超时，未结束。

本轮**不能标记整体提速或完整主控验收通过**：两稿到约 504 秒才写出，初稿前有 26 段临时像素扫描，检查工作被前置到脚本预算之外。当前工作区已补观察上限、禁止自写定位，未追加 Flash；旧冻结结果不改。阶段实施和试验报告完成，效率目标及新补充指令的真实行为仍待后续验证。

图层校验失败已向用户解释为“已识别照片，但在叠放列表漏了引用”，不是识图失败。新规则不能以结束为由将已知主要内容截断当作轻微问题；预算用完仍有重大问题时应返回 not_ready，而非继续循环或宣布可用。后续是否简化重复图层信息应另行评估。

最新证据见 20260917-stop-rules/RESULTS.md、review.md、stop-evidence.json、offline-tests-final.xml 和 preservation-check.json。停止规则更新暂留工作区，不额外创建第二次提交。


## 17. 图层信息去重方案（讨论稿，尚未实施）

用户要求思考如何改为“元素自身带排序信息，由程序生成完整图层”。本轮只读核对当前 analysis.py、预览及工程 schema，并记录建议；不修改运行代码、不提交、不测试或调用模型。本节不是当前已支持格式。

### 推荐的数据边界

- 模型草案顶层改为 background / slots / texts / overlays / questions，删除必填 layer_order。保持图片和可改文字分离，不退回混合元素清单。
- slots、texts 和独立 overlays 可写可选整数 z；越小越靠后，不要求连续或唯一。省略时采用文档化默认值，避免把“漏列表 ID”换成“漏 z 字段”的新报错。
- 建议缺省值：普通照片 100、独立装饰 200、可改文字 300。跨照片后置线条等可显式设更小值；这些是通用起点，不推断真实遮挡。背景固定为唯一最底层，不要求模型再给背景排序。
- 相同 z 稳定排序；同类保持原数组顺序，跨类按固定 slots → overlays → texts 次序打破平局。相同值本身不报错。不重叠对象无需主控精排；若同层重叠且缺少明确意图，给一个层级疑点，不触发逐项重查。
- Attachment 继续表达装饰所属照片。普通附属装饰不写全局 z，用 position=below/above 在照片前后自动展开。同侧多个附属装饰按 overlays 原数组顺序。
- 自动展开的“照片及附属装饰”是叠放单元，不意味着合并位图，也不将所属关系与抠图/素材生成混为一谈。

### 确定性展开

1. 收集所有 slots/texts/overlays 的真实定义，检查 ID 唯一和归属目标存在；不从另一份名单收集元素。
2. 处理背景：固定背景产生一个底层；客户背景槽从普通槽排序集合排除，唯一置底。
3. 独立元素按有效 z 稳定排序；每张照片默认展开为 below 装饰 → 照片 → above 装饰。每个定义恰好出现一次。
4. 工具生成 expanded_layer_order，供预览、摘要和后续工程使用。该输出不是模型输入，也不要求模型复制回草案。
5. 进入 project.json 时转换为有序 elements 数组，不额外在工程里维护 z。现有渲染器已经按数组顺序绘制，不需要重写渲染器。

### 外部穿插的例外

不能把所有装饰强制放在照片上方，也不能让照片与边框永远成为无法穿插的整体。需要“照片 → 独立装饰 → 边框”时，附属边框可以提供显式全局 z；此时 attachment 只保留照片归属用于同组移动，不再自动紧贴照片展开。

同一个装饰只能有一个有效排序来源：无 z 时用 attachment.position；有 z 时按显式全局 z。模型规范要求显式模式不重复写 position。若旧输入兼带两者，归一化结果明确记录采用 z、忽略的 position，不静默制造双重顺序或再要求模型维护第二份清单。预览分别展示“归属照片”和“实际层级”。

若层级意图本身无法判断，保留疑点，不使用坐标/颜色算法猜遮挡，也不为了消除疑点进入关系图/约束求解循环。

### 校验和停止规则衔接

保留 ID 重复、引用不存在、坐标非法、类型无效等真实结构错误；去掉 layer_order 缺漏/重复/附属项不得出现等整组同步校验。z 省略、同值、背景无需排序都不构成报错。

“程序不会漏掉已经定义的元素”与“模型是否识别漏了整个对象”不同；后者仍靠一次看图。自动补全层级不能证明视觉遮挡正确。之前的最多两次检查、相同输入复用、finish 终态和初稿前观察限制继续保留。

### 实施范围与验证建议

- scripts/collage_recreate/analysis.py：可选 z、背景/附属展开、生成完整顺序、简化校验；预览展示有效顺序来源。
- scripts/collage_recreate/localize.py：原样保留排序/归属信息，坐标细化不改变叠放意图。缓存仅因排序变化不应重做像素计算，但整份预览需更新；实施时核对当前缓存键的职责。
- analysis_session.py：z/attachment 变化属于实质草案变化，不能被“忽略诊断备注”的输入去重抹掉；不增加重试次数或新的主控阶段。
- SKILL.md 与 references/analysis.md、tools.md、project.md：明确草案输入和程序输出的边界，删除让模型手填图层总表的步骤。
- 旧草案不静默混用两套格式。旧实测及冻结版本保留；若需要迁移，提供显式转换并另存新文件，按旧 layer_order 的顺序分配间隔 z，保留 attachment。旧草案本身有缺漏时，迁移记录默认补入及疑点，不能伪称恢复了原本未知意图。
- 验证核心：照片自动出现且恰好一次；两种背景唯一置底；默认值/同 z 稳定；附属层前后展开；照片卡片互相遮挡时保持整组顺序；显式 z 可穿插但仍同组移动；已有非法引用仍报错；定位不改层级；工程输出顺序与预览一致。
- 先做这些有限离线用例，再对同样原参考开展新的获准行为验证，比较是否消除了重复登记型返工；不要为本次纯讨论自动启动测试或新外发。

该改动针对结构填写错误，不能单独解决上一轮初稿前 26 段临时像素扫描造成的耗时。前置观察边界仍需后续真实验证。

## 18. 用户调整方向：保留 layer_order，补全后校验（历史讨论；实施以第 19 节为准）

用户不采用第 17 节删 layer_order / 新增 z 的较大改造。此前提出先加强文字说明，随后进一步指出应按“无 attachment 元素全部列出、附属装饰自动展开”的规则补齐缺项。本轮已只读核对展开代码和最初模型草案；未改运行代码。

已确认：当前 expected 集合本来就是全部 slots + 全部 texts + attachment=null 的 overlays，再加固定背景（摄影背景槽属于 slots）。两份原始草案漏掉的是照片槽；照片槽本身没有 attachment。因此原校验报错符合既有规则。expanded_layers 只围绕 layer_order 中已经列出的照片插入附属层，不能凭空恢复缺失照片。

建议最小改动为“原始草案 → 确定性补全 → 展开附属层 → 完整性校验”，保持六字段格式，不引入 z：
- 保留已有合法独立元素的相对顺序，从真实定义计算必需集合，补入遗漏项。
- 附属装饰若误列在 layer_order 中，从独立清单去除并记录修复，随后只通过 attachment 展开一次；重复的同一列表引用可保留第一次并记录去重。实际定义重复 ID、引用不存在或类型不匹配仍报错，不猜测修复。
- 背景唯一置底；缺失照片优先插在现有非背景照片段之后，若无照片段，则插在第一段文字之前，无文字时放末尾。缺失独立装饰默认放文字之前，缺失文字放末尾。同类补入按原数组顺序。默认位置是明确的兜底，不声称程序由集合恢复了真实遮挡。
- 展开后所有真实元素必须出现且仅出现一次；输入 layer_order 只要求独立元素齐全，不能将“所有元素”误读为附属装饰也要再次列出。
- 原始草案不覆盖，补入/去重/移除附属列表项写入工具诊断与有效输出；有效预览与工程使用补全后的顺序。普通缺列表项在本次调用内修复，不要求模型为机械补录再花一次机会。
- 记录使用默认位置的对象；只对影响真实遮挡的疑点在一次预览中核对，不因为发生自动补全就再开一轮检查。

此方案尚待实施，当前代码仍为严格集合校验。第 17 节方案作为讨论历史保留，不执行。该修复处理重复登记导致的结构返工，不代替对初稿前过度观察的停止约束。

## 19. 用户最终选择：附属项先移除再展开（已实施并实测）

用户明确要求保留 layer_order，只强调“每个图片槽、文字、独立装饰必须出现且仅出现一次”，删除该段附属项排除/展开说明。工具负责从输入清单移除附属装饰，再根据 attachment 按 below → 照片 → above 展开，同侧按 overlays 顺序。第 17 节删列表/新增 z、第 18 节自动补齐漏照片均未采用。

实施于 analysis.py：独立层完整性、唯一性及背景位置保持校验，附属引用允许省略/重复/乱序；成功返回完整规范顺序，已展开输入可再次读取且不重复插层。refine 持久化有效草案，预览使用同一顺序，原始输入保留。未知引用/类型不符/无效 attachment 仍报错，没有猜测缺失照片的层级，也不增加检查阶段或次数。

用户追加明确指示“改完了之后用openclaw+Qwen3.8-flash试一下那两个例子…然后提交代码”，已据此完成新单次两图隔离实测，范围为此前明确批准的同两图分析/定位/停止，无生图、额外素材或全局配置修改。

- [x] 小范围规范、脚本、必要回归更新：139 passed in 17.15s；skill 格式通过。
- [x] 真实模型确认：17 次 responseModel 均 qwen3.8-flash；两图附属边框都显式列入输入，新校验接受，规范顺序每项一次，图层错误 0。
- [x] 停止行为：两图各一次有效 refine、一次预览、一次 finish；没有像素扫描或源码排错。174.625 秒 / 17 工具，相对上一轮 613.947 秒 / 59 工具，分别减少 71.6% / 71.2%。不是重复采样或单一因素的性能保证。
- [x] 如实记录例外：一条 shell 引号错误、一次 task 根目录错误；模型正常 stop，OpenClaw 退出仍有既有 cleanup 异常。
- [ ] 视觉和制作准备：A 装饰框未覆盖长连接线、星群/文字有裁缺，B 照片槽仍包含相框。模型均自评 usable_with_questions，独立复核不采纳为视觉通过。不能通过停止规则掩盖实质错误。
- [x] 报告与提交范围整理：本轮图层更新连同此前未提交停止规则一起提交；原始参考、模型响应和隔离产物留在仓库外。

证据入口：D:/codes/collage-recreate-work/20260917-attachment-order/RESULTS.md；两图原始输出和冻结 skill 保留，不补写模型结果。后续仅针对语义范围、照片内区和重大问题判定做有证据的改进，不恢复无止境检查。
