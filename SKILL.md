---
name: collage-recreate
description: 根据一张静态参考图、客户照片和文案，制作可换图、改字、移动主要元素的分层静态拼贴海报。适用于 OpenClaw 的静态拼图复刻与后续修改，不处理视频或时间轴。
---

# 静态拼图复刻

交付 PNG 成图和能重新渲染的工程。主控使用 OpenClaw + qwen3.8-flash；需要生成素材时，由本工具调用 qwen-image-3.0-pro。

命令入口为 `{baseDir}/scripts/run.py`（本 skill 目录下的脚本），使用已安装 `requirements.txt` 的 Python。安装和命令参数见 [工具说明](references/tools.md)。建立或修改布局时读 [静态工程格式](references/project.md)；只有生图或请求恢复时读 [图像服务](references/service.md)。

## 制作

1. **制作草案与定位。** 首次分析或参考变化时读 [草案规范](references/analysis.md)，看完整参考并按需看局部。摄影背景优先客户素材，设计纹理底板才考虑清版。图片 slots、文字 texts 分开；同归属、同移动和遮挡的装饰合成独立制作组合。写 analysis/draft.json 的粗 source_rect，用 refine_layout.py 批量细化并实际看对照预览；不确定项保留待复核，原草案不覆盖。只要求分析时完成本步即返回；已有草案适用时复用。
2. **建立独立任务。** 按草案及已确认的用户内容，用 request.json 和 `init` 导入；已有任务先 `inspect` 并读 project.json。布局用稳定 ID；所有元素写画布坐标，分组仅增加整体 dx/dy。
3. **准备素材。** 现有照片、准确文字和普通几何用确定性工具；需要关键手绘质感时生成独立装饰、纹理或背景。授权范围及调用上限写入新任务，生成只上传列入权限的必要资源。整张生成图不能替代分层工程。
4. **先验证关键素材。** 每次 `generate`/恢复后实际打开返回的图片，检查内容、边缘和风格；alpha_range=[255,255] 表示完全不透明，PNG 后缀或白底均不能证明透明。核心质感不成立时先处理素材，不向后包装为成品。
5. **组装与修正。** 用 `apply` 写入图片/文字层和分组，`render` 后打开结果，与参考对照。照片、标签、边框需要一起移动时纳入同一组。优先修正最影响画面的差异，保留具体观察。
6. **交付。** 正式尺寸再次看图；用 `export` 输出新目录，并从其 runtime 重新渲染。说明可替换的照片/文字/元素组和不可细分的位图笔触。不可分发字体通过字体需求清单和显式 --font 绑定提供。

## 已有工程的修改

换照片使用 `apply` 的 assets；改文案使用 texts；移动组使用 group_updates。每次提供当前 expected_revision，避免覆盖其他修改。render/check/apply 不会自动生图；不用重新生成未变化的素材。

示例：

```json
{"expected_revision":2,"texts":{"title":"新的标题"},"group_updates":{"card":{"dx":12,"dy":-8}}}
```

## 停止与证据

- 缺图、缺字形、文字溢出、错误引用或无意越界：修正输入/布局后再渲染，不静默换字体或截字。
- 服务状态 submitted/unknown：核对原请求；不得以更改提示词、ID 或任务名绕过防重发。已有响应下载失败只做 resume。
- 达到任务调用上限或约定修正轮数：保存候选及未解决差异，报告当前状态。
- 看不到参考、生成图或结果：说明能力阻塞，不宣称视觉通过。
- 技术检查、服务成功、OpenClaw 实际调用、视觉和可编辑性分别报告。所有工具输出默认 visual_status=unreviewed；实际看图记录和用户反馈才支持视觉结论。

保留任务开始时间、首个预览时间、服务次数/等待、主要修正和人工介入。开发用 AGENTS.md、STATUS.md、PLAN.md 不属于运行时必读材料。
